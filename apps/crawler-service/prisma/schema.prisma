generator client {
  provider = "prisma-client-js"
}

generator apiEngineClient {
  provider        = "prisma-client-js"
  previewFeatures = ["driverAdapters"]
  output          = "../../api-engine/node_modules/.prisma/client"
}

datasource db {
  provider = "postgresql"
}

model StockPrice {
  id        Int      @id @default(autoincrement())
  symbol    String
  time      DateTime
  open      Float
  high      Float
  low       Float
  close     Float
  volume    BigInt
  createdAt DateTime @default(now())

  @@unique([symbol, time])
}

model AiAnalysis {
  id        Int      @id @default(autoincrement())
  symbol    String
  content   String   @db.Text
  createdAt DateTime @default(now())
}